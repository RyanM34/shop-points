server:
  port: 8080

app:
  config:
    api-version: v1

spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password: sa
  h2:
    console:
      enabled: true

logging:
  level:
    root: info
  config: classpath:logback-spring.xml